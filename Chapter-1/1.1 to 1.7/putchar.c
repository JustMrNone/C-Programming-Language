#include <stdio.h>

int main()
{
    //reads next input character from the text stream 
    char c = getchar();
    //prints a character each time that it is called
    putchar(c);
    //using printf to do the samething 
    printf("%c", c);
}